version: '2'
services:
    gateway_pathway-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/gateway_pathway/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=gateway_pathway
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
