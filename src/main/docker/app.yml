version: '2'
services:
    gateway_pathway-app:
        image: gateway_pathway
        environment:
            - SPRING_PROFILES_ACTIVE=prod,swagger
            - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://admin:$${jhipster.registry.password}@jhipster-registry:8761/eureka
            - SPRING_CLOUD_CONFIG_URI=http://admin:$${jhipster.registry.password}@jhipster-registry:8761/config
            - SPRING_DATASOURCE_URL=jdbc:postgresql://gateway_pathway-postgresql:5432/gateway_pathway
            - JHIPSTER_SLEEP=30 # gives time for the JHipster Registry to boot before the application
            - SPRING_DATA_ELASTICSEARCH_CLUSTER_NODES=gateway_pathway-elasticsearch:9300
        ports:
            - 8080:8080
    gateway_pathway-postgresql:
        extends:
            file: postgresql.yml
            service: gateway_pathway-postgresql
    gateway_pathway-elasticsearch:
        extends:
            file: elasticsearch.yml
            service: gateway_pathway-elasticsearch
    jhipster-registry:
        extends:
            file: jhipster-registry.yml
            service: jhipster-registry
        environment:
            - SPRING_CLOUD_CONFIG_SERVER_NATIVE_SEARCH_LOCATIONS=file:./central-config/docker-config/
